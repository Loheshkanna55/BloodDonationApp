<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Donation & Request History</title>
    <link rel="stylesheet" href="/css/admin-home.css">
    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #f8f9fa;
            margin: 0;
            padding: 0;
        }

        

        .history-container {
            display: flex;
            flex-wrap: wrap;
            justify-content: center;
            gap: 20px;
            margin-top: 40px;
        }

        .history-card {
            display: flex;
            align-items: center;
            background: #fff;
            border-radius: 12px;
            box-shadow: 0px 4px 8px rgba(0, 0, 0, 0.1);
            padding: 15px;
            width: 600px;
            gap: 15px;
            transition: transform 0.3s ease;
            border-left: 5px solid #d32f2f;
        }

        .history-card:hover {
            transform: translateY(-5px);
        }

        .profile-pic {
            width: 70px;
            height: 70px;
            border-radius: 50%;
            object-fit: cover;
            border: 2px solid #d32f2f;
        }

        .history-details {
            flex-grow: 1;
            display: flex;
            flex-direction: column;
        }

        .header {
            display: flex;
            align-items: center;
            gap: 10px;
        }

        .user-name {
            font-size: 18px;
            font-weight: bold;
            color: #222;
            padding-right: 10px;
            border-right: 2px solid #ccc;
        }

        .request-type {
            font-size: 14px;
            font-weight: bold;
            color: #d32f2f;
        }

        .info {
            font-size: 14px;
            color: #555;
            margin-top: 5px;
        }

        .status-badge {
            display: flex;
            align-items: center;
            font-size: 16px;
            font-weight: bold;
            padding: 6px 12px;
            border-radius: 20px;
            color: white;
        }

        .accepted {
            background-color: #4CAF50;
        }
       
    </style>
</head>
<body>

    <%- include('partials/header') %>

    <div class="history-container">
        <% history.forEach(entry => { %>
            <div class="history-card">
                <img src="<%= entry.profilePic %>" alt="Profile Picture" class="profile-pic">
                
                <div class="history-details">
                    <div class="header">
                        <div class="user-name"><%= entry.name %></div>
                        <div class="request-type"><%= entry.icon %> <%= entry.type %></div>
                    </div>
                    <div class="info">
                        <strong>Age:</strong> <%= entry.age %>
                    </div>
                    <div class="info"><strong>Location:</strong> <%= entry.location %></div>
                    <div class="info"><strong>Blood Type:</strong> <%= entry.bloodType %></div>

                    <% if (entry.type === "Blood Donation") { %>
                        <div class="info"><strong>Health Condition:</strong> <%= entry.healthCondition %></div>
                        <div class="info"><strong>Blood Amount:</strong> <%= entry.donationAmount %> </div>
                    <% } else { %>
                        <div class="info"><strong>Reason:</strong> <%= entry.reason %></div>
                        <div class="info"><strong>Blood Amount:</strong> <%= entry.bloodAmount %> </div>
                    <% } %>
                </div>

                <div class="status-badge accepted">✔ Completed</div>
            </div>
        <% }) %>
    </div>

</body>
</html>
