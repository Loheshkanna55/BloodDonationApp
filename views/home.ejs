<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Home</title>
  
</head>
<body>
  <%- include('partials/home-header') %>
  

  <% if (message) { %>
    <div class="alert" id="alert-box"><%= message %></div>
  <% } %>
  <div class="hero-section">
    <h2>Welcome, <%= user ? user.username : 'Guest' %>!</h2>
    <p class="hero-text">
        Donate blood. Save lives.
    </p>
  </div>
  

  <!-- Options Container -->
  <div class="options-container">
    <div class="option-card">
        <img src="/images/blood-donation.png" alt="Donate Blood">
        <h3>Donate Blood</h3>
        <p>Give the gift of life. Every drop counts!</p>
        <a href="/donate" class="btn" id="donate-btn">Donate Now</a>
    </div>
    
    <div class="option-card">
        <img src="/images/blood-receiving.png" alt="Receive Blood">
        <h3 class="receive_head">Receive Blood</h3>
        <p>Need blood? Find donors easily from us.</p>
        <a href="/receive" class="btn" id="request-btn">Request Blood</a>
    </div>
  </div>

  <!-- Contact Us Section -->
  

  <%- include('partials/footer') %>
</body>
</html>

<script>
  
  setTimeout(() => {
        const alertBox = document.getElementById("alert-box");
        if (alertBox) {
          alertBox.style.display = "none";
        }
      }, 10000);
      // Remove message from URL without refreshing
      
</script>
<style>
  /* General Page Styling */
  body {
  font-family: Arial, sans-serif;
  background: #f8f9fa; /* Gentle background */
  text-align: center;
  align-items: center;
  padding: 0;
  margin: 0;
}

.hero-section {
  background: #f0f0f0;
  padding: 30px;
  border-radius: 10px;
  margin: 20px auto;
  width: 60%;
  box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
}

.hero-section h2 {
  font-size: 26px;
  color: #333;
}

.hero-text {
  font-size: 16px;
  font-weight: bold;
  color: #d50000;
}

/* Options Container */
.options-container {
  display: flex;
  justify-content: center;
  gap: 20px;
  flex-wrap: wrap;
  margin-top: 30px;
}

.option-card {
  background: white;
  padding: 20px;
  border-radius: 10px;
  box-shadow: 0 4px 8px rgba(0, 0, 0, 0.2);
  text-align: center;
  width: 250px;
}



.btn {
  display: inline-block;
  padding: 10px 15px;
  background: #ff4757;
  color: white;
  text-decoration: none;
  border-radius: 5px;
  margin-top: 10px;
}

.btn:hover {
  background: #e84118;
}


#receive-btn{
 margin-top: 37px;
}
.receive-div{
  margin-top: 30px;
  margin-bottom: -10px;
}
.receive_head{
  margin-top: 30px;
}

  .alert {
    background-color: #ffcccc;
    color: #d8000c;
    padding: 15px;
    border-radius: 5px;
    text-align: center;
    margin: 15px auto;
    width: 80%;
    font-size: 18px;
    font-weight: bold;
    animation: fadeIn 0.5s ease-in-out;
  }

  @keyframes fadeIn {
    from { opacity: 0; }
    to { opacity: 1; }
  }
  
  
</style>