<<<<<<< HEAD
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Schedule History</title>
  <link rel="stylesheet" href="/css/admin.css">
  <style>
    body {
      font-family: 'Poppins', sans-serif;
      background: #f8f9fa;
      text-align: center;
    }
    .container {
      max-width: 900px;
      margin: 30px auto;
    }
    .card {
      background: white;
      padding: 20px;
      margin-bottom: 15px;
      border-radius: 10px;
      box-shadow: 0px 4px 8px rgba(0, 0, 0, 0.15);
      text-align: left;
      display: flex;
      align-items: center;
      border-left: 8px solid #d9534f; /* Left border for visual effect */
      transition: transform 0.3s ease, box-shadow 0.3s ease;
    }
    
    .profile-pic {
      width: 90px;
      height: 90px;
      border-radius: 50%;
      margin-right: 20px;
      border: 3px solid #d9534f; /* Profile image border */
    }
    .details {
      flex-grow: 1;
    }
    .details h3 {
      color: #d9534f;
      font-size: 20px;
      margin-bottom: 5px;
    }
    .details p {
      font-size: 14px;
      color: #555;
      margin: 5px 0;
    }
    .admin-buttons {
      margin-top: 10px;
    }
    .btn {
      display: inline-block;
      padding: 8px 12px;
      margin: 5px;
      background: #d9534f;
      color: white;
      text-decoration: none;
      border-radius: 5px;
      cursor: pointer;
      border: none;
      font-weight: bold;
      transition: background 0.3s ease;
    }
    .btn:hover {
      background: #c9302c;
    }
    .btn.add-entry {
      background: #5bc0de;
    }
    .btn.add-entry:hover {
      background: #31b0d5;
    }
   
  </style>
</head>
<body>
  <%- include('partials/header') %>
  <div class="container">
    <h2>Scheduled Donations</h2>
    
    <% schedules.forEach(schedule => { %>
      <div class="card">
        <img src="/images/<%= schedule.gender === 'Male' ? 'man.png' : 'woman.png' %>" alt="Profile" class="profile-pic">
        <div class="details">
          <h3><%= schedule.name %></h3>
          <p><strong>🩸 Donation Amount:</strong> <%= schedule.donationAmount %></p>
          <p><strong>🩸 Blood Type:</strong> <%= schedule.bloodType %></p>
          <p><strong>📍 Donation Center:</strong> <%= schedule.address %></p>
          <p><strong>📞 Phone:</strong> <%= schedule.phone %></p>
          <p><strong>📅 Fixed Date:</strong> <%= schedule.formattedScheduleDate %></p>
          <p><strong>⏳ Time Slot:</strong> <%= schedule.timeSlot %></p>
          <p><strong>💉 Blood Pressure:</strong> <%= schedule.bloodPressure %></p>
          
          <div class="admin-buttons">
            <button class="btn add-entry" data-id="<%= schedule._id %>">➕ Add Entry</button>
            <button class="btn remove-entry" data-id="<%= schedule._id %>">❌ Remove Entry</button>
          </div>
        </div>
      </div>
    <% }); %>
  </div>

  <script>
    document.addEventListener("DOMContentLoaded", function () {
      // Handle Add Entry
      document.querySelectorAll(".add-entry").forEach(button => {
        button.addEventListener("click", function () {
          const scheduleId = this.getAttribute("data-id");
  
          if (confirm("Are you sure you want to add this blood to the blood bank?")) {
            fetch(`/admin/add-blood/${scheduleId}`, { method: "POST" })
              .then(response => response.json())
              .then(data => {
                if (data.success) {
                  alert("Blood added successfully and schedule removed!");
                  location.reload();
                } else {
                  alert("Error: " + data.message);
                }
              })
              .catch(error => console.error("Error:", error));
          }
        });
      });
  
      // Handle Remove Entry
      document.querySelectorAll(".remove-entry").forEach(button => {
        button.addEventListener("click", function () {
          const scheduleId = this.getAttribute("data-id");
  
          if (confirm("Are you sure you want to remove this schedule entry?")) {
            fetch(`/admin/remove-schedule/${scheduleId}`, { method: "DELETE" })
              .then(response => response.json())
              .then(data => {
                if (data.success) {
                  alert("Schedule entry removed successfully!");
                  location.reload();
                } else {
                  alert("Error: " + data.message);
                }
              })
              .catch(error => console.error("Error:", error));
          }
        });
      });
    });
  </script>
</body>
</html>
=======
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Schedule History</title>
  <link rel="stylesheet" href="/css/admin.css">
  <style>
    body {
      font-family: 'Poppins', sans-serif;
      background: #f8f9fa;
      text-align: center;
    }
    .container {
      max-width: 900px;
      margin: 30px auto;
    }
    .card {
      background: white;
      padding: 20px;
      margin-bottom: 15px;
      border-radius: 10px;
      box-shadow: 0px 4px 8px rgba(0, 0, 0, 0.15);
      text-align: left;
      display: flex;
      align-items: center;
      border-left: 8px solid #d9534f; /* Left border for visual effect */
      transition: transform 0.3s ease, box-shadow 0.3s ease;
    }
    
    .profile-pic {
      width: 90px;
      height: 90px;
      border-radius: 50%;
      margin-right: 20px;
      border: 3px solid #d9534f; /* Profile image border */
    }
    .details {
      flex-grow: 1;
    }
    .details h3 {
      color: #d9534f;
      font-size: 20px;
      margin-bottom: 5px;
    }
    .details p {
      font-size: 14px;
      color: #555;
      margin: 5px 0;
    }
    .admin-buttons {
      margin-top: 10px;
    }
    .btn {
      display: inline-block;
      padding: 8px 12px;
      margin: 5px;
      background: #d9534f;
      color: white;
      text-decoration: none;
      border-radius: 5px;
      cursor: pointer;
      border: none;
      font-weight: bold;
      transition: background 0.3s ease;
    }
    .btn:hover {
      background: #c9302c;
    }
    .btn.add-entry {
      background: #5bc0de;
    }
    .btn.add-entry:hover {
      background: #31b0d5;
    }
   
  </style>
</head>
<body>
  <%- include('partials/header') %>
  <div class="container">
    <h2>Scheduled Donations</h2>
    
    <% schedules.forEach(schedule => { %>
      <div class="card">
        <img src="/images/<%= schedule.gender === 'Male' ? 'man.png' : 'woman.png' %>" alt="Profile" class="profile-pic">
        <div class="details">
          <h3><%= schedule.name %></h3>
          <p><strong>🩸 Donation Amount:</strong> <%= schedule.donationAmount %></p>
          <p><strong>🩸 Blood Type:</strong> <%= schedule.bloodType %></p>
          <p><strong>📍 Donation Center:</strong> <%= schedule.address %></p>
          <p><strong>📞 Phone:</strong> <%= schedule.phone %></p>
          <p><strong>📅 Fixed Date:</strong> <%= schedule.formattedScheduleDate %></p>
          <p><strong>⏳ Time Slot:</strong> <%= schedule.timeSlot %></p>
          <p><strong>💉 Blood Pressure:</strong> <%= schedule.bloodPressure %></p>
          
          <div class="admin-buttons">
            <button class="btn add-entry" data-id="<%= schedule._id %>">➕ Add Entry</button>
            <button class="btn remove-entry" data-id="<%= schedule._id %>">❌ Remove Entry</button>
          </div>
        </div>
      </div>
    <% }); %>
  </div>

  <script>
    document.addEventListener("DOMContentLoaded", function () {
      // Handle Add Entry
      document.querySelectorAll(".add-entry").forEach(button => {
        button.addEventListener("click", function () {
          const scheduleId = this.getAttribute("data-id");
  
          if (confirm("Are you sure you want to add this blood to the blood bank?")) {
            fetch(`/admin/add-blood/${scheduleId}`, { method: "POST" })
              .then(response => response.json())
              .then(data => {
                if (data.success) {
                  alert("Blood added successfully and schedule removed!");
                  location.reload();
                } else {
                  alert("Error: " + data.message);
                }
              })
              .catch(error => console.error("Error:", error));
          }
        });
      });
  
      // Handle Remove Entry
      document.querySelectorAll(".remove-entry").forEach(button => {
        button.addEventListener("click", function () {
          const scheduleId = this.getAttribute("data-id");
  
          if (confirm("Are you sure you want to remove this schedule entry?")) {
            fetch(`/admin/remove-schedule/${scheduleId}`, { method: "DELETE" })
              .then(response => response.json())
              .then(data => {
                if (data.success) {
                  alert("Schedule entry removed successfully!");
                  location.reload();
                } else {
                  alert("Error: " + data.message);
                }
              })
              .catch(error => console.error("Error:", error));
          }
        });
      });
    });
  </script>
</body>
</html>
>>>>>>> 4c5af07 (Added Docker File)
